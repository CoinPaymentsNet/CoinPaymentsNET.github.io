get:
  tags:
    - Callbacks
  summary: get deposits by callback address id
  description: |
    get deposits by callback address id
  operationId: getDepositsByCallbackAddressId
  parameters:
    - name: id
      in: path
      required: true
      schema:
        $ref: ../../components/globalTypes/id.yaml
  responses:
    '200':
      description: Success status code ( 200 )
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/callbacks/deposits.yaml
    '400':
      description: Bad request example!


