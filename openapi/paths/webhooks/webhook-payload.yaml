post:
  tags:
    - Invoice Webhooks API
  summary: Notification Payload
  description: |
    All webhooks sent by CPs API contain information on the event they signalize about and information on the invoice 
    and payment to which the event refers. Below is the payload of the webhooks sent from CPs API to your server API.
  operationId: notificationPayload
  responses:
    '200':
      description: Success status code ( 200 )
      content:
        application/json:
          schema:
            $ref: ../../components/responses/Webhooks/notification-payload.yaml
    '400':
      description: CoinPayments API could not reach your server