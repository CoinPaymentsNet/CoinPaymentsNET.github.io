type: object
properties:
  transactionFees:
    $ref: ./fees.yaml
  networkFee:
    description:  An object with the same fields as the fees object in the "transactionFee"
    $ref: ./simulateFees.yaml
  conversionFee:
    description:  An object with the same fields as the fees object in the "transactionFee"
    $ref: ./simulateFees.yaml
