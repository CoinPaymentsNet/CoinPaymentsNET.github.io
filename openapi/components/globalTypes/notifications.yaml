type: array
description: |
  specify the event name you want your server to be notified about when it occures

  e.g. send notification when an invoice is created
items:
  type: string
example: [
  'invoiceCreated',
  'invoicePending',
  'invoicePaid',
  'invoiceCompleted',
  'invoiceCancelled'
]
